version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - mvn install
artifacts:
  files:
    - target/codedeploy.war
    - appspec.yml
    - scripts
  discard-paths: yes

version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk11
  build:
    commands:
      - mvn package
artifacts:
  files:
    - target/codedeploy-1.0.0.jar
    - appspec.yml
  discard-paths: yes